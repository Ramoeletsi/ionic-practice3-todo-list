import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-practice3-Todo-list',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
